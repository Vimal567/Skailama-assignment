# Event Management (MERN) — Assignment

MERN stack Event Management app (no auth).
Stack:
- Frontend: React (Vite recommended) + Zustand
- Backend: Express.js
- Database: MongoDB (Mongoose)
- Timezone handling: dayjs + dayjs/plugin/timezone + dayjs/plugin/utc
- Styling: Vanilla CSS

See `frontend/` and `backend/` folders for code. Follow the README sections in the root for setup.
